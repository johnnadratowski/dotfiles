for plugin in $HOME/scripts/startup/*
do
    if [ -f $plugin ]; then
      source $plugin
    fi
done

if [ -d $HOME/local-startup ]
then
    for plugin in $HOME/local-startup/*
    do
      if [ -f $plugin ]; then
        source $plugin
      fi
    done
fi
