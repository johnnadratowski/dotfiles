{
  "$schema": "https://www.schemastore.org/claude-code-keybindings.json",
  "$docs": "https://code.claude.com/docs/en/keybindings",
  "$note": "No custom bindings. scroll:bottom is already Ctrl+End by default; ctrl+e was reclaimed for end-of-line. This file is kept as the symlinked scaffold for future overrides.",
  "bindings": []
}
