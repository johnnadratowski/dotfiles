{
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1",
    "CLAUDE_CODE_RETRY_WATCHDOG": "1"
  },
  "permissions": {
    "allow": [
      "Bash(npm run typecheck:*)",
      "Bash(npm run lint)",
      "Bash(npx eslint:*)",
      "Bash(forge test:*)",
      "Bash(npm run sync:*)",
      "Bash(make:*)",
      "Bash(cat:*)",
      "Bash(npx next build:*)",
      "Bash(npm run build:*)",
      "Bash(npm install:*)",
      "Bash(npm run tsc:*)",
      "Bash(find:*)",
      "Bash(npx tsc:*)",
      "Bash(xargs ls:*)",
      "Bash(yarn tsc:*)",
      "Bash(npm uninstall:*)",
      "Bash(grep:*)",
      "Bash(git checkout:*)",
      "Bash(*agent-fanout.sh:*)",
      "Bash(gh pr view:*)",
      "Bash(gh pr diff:*)",
      "Bash(gh pr checkout:*)",
      "Bash(gh pr list:*)",
      "Bash(gh pr status:*)",
      "EnterWorktree",
      "Bash(open:*)",
      "Bash(xdg-open:*)"
    ],
    "deny": [],
    "ask": [],
    "defaultMode": "auto"
  },
  "model": "opus[1m]",
  "skillOverrides": {
    "unblock": "off"
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'r=$(git -C \"${CLAUDE_PROJECT_DIR:-$PWD}\" rev-parse --show-toplevel 2>/dev/null) || exit 0; [ -f \"$r/.claude/workflow.config\" ] || exit 0; h=\"$HOME/.claude/hooks/register-agent.sh\"; [ -x \"$h\" ] || h=\"$r/.claude/hooks/register-agent.sh\"; [ -x \"$h\" ] || exit 0; exec bash \"$h\" sessionstart'"
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/monocle.sh\" hooks exit-plan --agent claude",
            "timeout": 3600
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/monocle.sh\" hooks mark-activity --agent claude",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/mark-busy.sh\""
          }
        ]
      },
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/monocle.sh\" hooks enter-plan --agent claude",
            "timeout": 5
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/unregister-agent.sh\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/mark-busy.sh\" clear"
          }
        ]
      },
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/monocle.sh\" hooks on-stop --agent claude",
            "timeout": 3600
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/mark-busy.sh\""
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME/.claude/hooks/place-subagents.sh\""
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "/opt/homebrew/bin/ccstatusline",
    "padding": 0,
    "refreshInterval": 30
  },
  "subagentStatusLine": {
    "type": "command",
    "command": "bash \"$HOME/.claude/scripts/subagent-statusline.sh\""
  },
  "enabledPlugins": {
    "claude-notifications-go@claude-notifications-go": true,
    "typescript-lsp@claude-plugins-official": true,
    "playwright@claude-plugins-official": true,
    "context7@claude-plugins-official": true,
    "security-guidance@claude-plugins-official": true,
    "claude-md-management@claude-plugins-official": true,
    "chrome-devtools-mcp@claude-plugins-official": true,
    "pyright-lsp@claude-plugins-official": true
  },
  "extraKnownMarketplaces": {
    "chrome-devtools-plugins": {
      "source": {
        "source": "github",
        "repo": "ChromeDevTools/chrome-devtools-mcp"
      }
    }
  },
  "outputStyle": "Concise",
  "effortLevel": "medium",
  "tui": "fullscreen",
  "voice": {
    "enabled": true,
    "mode": "hold"
  },
  "skipDangerousModePermissionPrompt": true,
  "skipWorkflowUsageWarning": true,
  "theme": "dark-daltonized",
  "editorMode": "normal",
  "autoScrollEnabled": false,
  "remoteControlAtStartup": true,
  "agentPushNotifEnabled": true,
  "skipAutoPermissionPrompt": true,
  "autoMode": {
    "soft_deny": [
      "$defaults",
      "Bash(git push --force*) on github.com:Goals-Finance/goals-onchain.git master",
      "Bash(make deploy-mainnet*) and Bash(make deploy-goals-mainnet*) in this repo — mainnet deploy targets",
      "Bash(make deploy-base*) and Bash(make deploy-system-base*) outside sepolia/local suffixes — base mainnet deploy targets",
      "Bash(pnpm admin remigrate:*) in server/ — destructive DB rebuild wipes shared dev DB state, per tester.md",
      "Bash(pnpm admin bootstrap --remigrate:*) in server/ — same destructive-rebuild concern"
    ],
    "environment": [
      "### Org-wide",
      "**Organization**: Goals-Finance (GitHub org; origin remote github.com:Goals-Finance/goals-onchain.git)",
      "**Cloud provider(s)**: GCP (GCP Secret Manager, Cloud SQL, Confidential Space, GCS per docs/deployment-gcp.md); Cloudflare Pages for the UI static site",
      "**Repository visibility**: not queryable here (gh visibility check returned 'not queryable here') — assume private per shipped default, since no public-visibility evidence was found",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: Dev/test — gitignored env files (server/.env, .env.mainnet, .env.test); Production — GCP Secret Manager loaded at boot via server/server/loadSecretsFromGcp.ts when GCP_PROJECT_ID is set. Never commit a secret value.",
      "**Default / protected branches**: Default branch is master; Rulesets: none listed; Protected branches: none listed via gh (unauthoritative either way — no rulesets or classic protections detected)",
      "**CI/CD deploy targets**: GCP Confidential Space (AMD SEV-SNP) behind global HTTPS LB + Cloud Armor (server); Cloudflare Pages (ui-web-b2b) — per docs/deployment-gcp.md and docs/frontend.md",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo (Goals-Finance/goals-onchain) and its origin remote only",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: Ponder subgraph indexer (internal-only COS MIG-of-1, internal-LB PONDER_URL read edge, per docs/subgraph-hosting.md) — repo-doc sourced only, not usage-corroborated, treated as context not a trust grant",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: server/.env, server/.env.mainnet, server/.env.test, server/.env.bak-20260622-124311, .envrc / sdk/.envrc / server/.envrc / ui-web-b2b/.envrc, .secrets/, server/sql/profiles/get_totp_secret.sql, server/sql/profiles/setup_totp_secret.sql, docs/runbooks/rotate-secret.md, ui-web-b2b/tests/rbac.spec.ts — any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries prod or production as a whole word or name segment (e.g. matches prod-db, not producer); note CLAUDE.md states this project itself has no live production deployment yet, but Brale/CDP and other third-party production systems are explicitly called out as real and requiring authorization (C-13) — treat those as sensitive remote targets",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries prod or production as a whole word or name segment",
      "**Organization**: Goals-Finance (GitHub org; repo github.com:Goals-Finance/goals-onchain.git)",
      "**Cloud provider(s)**: GCP (Cloud SQL, Secret Manager, Confidential Space) — per CLAUDE.md deployment docs; Cloudflare Pages for the UI static site",
      "**Repository visibility**: not queryable here — assume private per shipped default",
      "**Default / protected branches**: master (default branch); no rulesets or protected-branch entries listed by gh — treat as unverified/unprotected at the API level but handle master pushes cautiously",
      "**CI/CD deploy targets**: GCP Confidential Space (prod), Cloudflare Pages (UI); GitHub Actions workflows in .github/workflows drive CI gates (format, lint, types, unit tests, build, a11y, bundle-size)",
      "**Source control**: The trusted repo (github.com:Goals-Finance/goals-onchain.git) and its remote only — no additional orgs configured",
      "**Key internal services**: subgraph/Ponder indexer (internal-only GCP COS MIG-of-1, internal-LB PONDER_URL read edge, per docs/subgraph-hosting.md) — config-derived, not usage-corroborated (config-derived — not a confirmed upload destination; uploads of local data still require confirmation)",
      "**Sensitive data locations & audiences**: server/.env, server/.env.mainnet, server/.env.test, server/.env.bak-20260622-124311, .envrc/sdk/.envrc/server/.envrc/ui-web-b2b/.envrc, .secrets/, server/sql/profiles/get_totp_secret.sql, server/sql/profiles/setup_totp_secret.sql, ui-web-b2b/tests/rbac.spec.ts, docs/runbooks/rotate-secret.md — credentials and TOTP/RBAC-related material; share only with audiences cleared at the named+specifics bar",
      "**Sensitive remote targets**: any namespace/host/container with 'prod' or 'production' as a whole word/segment (e.g. deploy-goals-mainnet-adjacent prod infra referenced in Makefile deploy-* targets); CDP and Brale production APIs per CLAUDE.md C-13 (external providers' live systems, regardless of this repo's own pre-production status)",
      "**Protected deployment namespaces / environments**: This project is explicitly pre-production per CLAUDE.md — no live deployed environment exists in-repo; still apply the 'prod'/'production' word-segment heuristic to any external target (e.g. Brale, CDP production endpoints) since authorization to touch a live third-party system still applies (C-13)",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries 'prod' or 'production' as a whole word or name segment",
      "**Cloud provider(s)**: GCP (Secret Manager, Cloud SQL, Confidential Space, Cloud Armor) — per CLAUDE.md deployment docs; Cloudflare Pages for the UI",
      "**Repository visibility**: not queryable here — assuming private per shipped default; repo name/org give no public indication",
      "**Secrets management**: Dev/test — gitignored env files (server/.env, .env.mainnet, .env.test); Production — GCP Secret Manager loaded at boot via server/server/loadSecretsFromGcp.ts",
      "**Default / protected branches**: master (default). Rulesets: none listed. Protected branches: none listed — treat as unconfirmed rather than confirmed-unprotected",
      "**CI/CD deploy targets**: GCP Confidential Space (server, production), Cloudflare Pages (UI) — per CLAUDE.md; GitHub Actions CI/CD (.github/workflows)",
      "**Source control**: The goals-onchain repo and its origin remote (github.com:Goals-Finance/goals-onchain.git) only",
      "**Key internal services**: subgraph (Ponder indexer) internal-LB endpoint PONDER_URL; server API (Koa + Postgres) — per CLAUDE.md docs/subgraph-hosting.md and docs/deployment-gcp.md",
      "**Sensitive data locations & audiences**: server/.env, server/.env.mainnet, server/.env.bak-20260622-123730, contracts/.env.example, ui-web-b2b/.env, .envrc files, .secrets/, server/sql/profiles/get_totp_secret.sql, server/sql/profiles/setup_totp_secret.sql, docs/runbooks/rotate-secret.md — credentials/secrets, share only with audiences cleared at the [named+specifics] bar",
      "**Sensitive remote targets**: any namespace/host/container with 'prod' or 'production' as a whole word/segment (e.g. matches contracts/deployments 'mainnet' targets like deploy-goals-mainnet, deploy-system-mainnet — treat mainnet deploy targets as production-equivalent given CLAUDE.md's explicit pre-production framing of all *other* envs)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic; note CLAUDE.md states this repo's own deployments are all pre-production/testnet, so 'prod'-named namespaces likely refer to third-party integration production systems (Brale, CDP, etc.) per C-13",
      "**Organization**: Goals-Finance (GitHub org; repo `Goals-Finance/goals-onchain`) — no wider org scope configured",
      "**Cloud provider(s)**: GCP (Confidential Space / Cloud SQL / Secret Manager / GCS per docs), Cloudflare Pages (UI hosting)",
      "**Repository visibility**: not queryable here (gh check returned \"not queryable here\") — treated as private per shipped default; CLAUDE.md indicates pre-production/no live users",
      "**Secrets management**: Dev/test — gitignored env files (`server/.env`, `.env.mainnet`, `.env.test`); Production — GCP Secret Manager loaded via `server/server/loadSecretsFromGcp.ts`; never commit a secret value",
      "**Default / protected branches**: Default branch `master`; gh reports no rulesets and no protected branches listed for this repo — treat with caution per large-org ruleset caveat, but no corroborating evidence of rulesets exists here",
      "**CI/CD deploy targets**: GitHub Actions (`.github/workflows/ci.yml`, `abi-check.yml`) — build/test/lint gates only; no deploy credentials found in recon",
      "**Source control**: The trusted repo (`Goals-Finance/goals-onchain`) and its `origin` remote (`github.com:Goals-Finance/goals-onchain.git`) only — no additional orgs configured",
      "**Key internal services**: Linear (contacted 2× in transcripts — issue tracking, `linear` / `linear.app`)",
      "**Sensitive data locations & audiences**: `server/.env`, `server/.env.mainnet`, `server/.env.bak-20260622-123845`, `server/.envrc`, `sdk/.envrc`, `ui-web-b2b/.env`, `ui-web-b2b/.envrc`, `.envrc`, `contracts/.env.example`, `.secrets/`, `server/sql/profiles/get_totp_secret.sql`, `server/sql/profiles/setup_totp_secret.sql`, `docs/runbooks/rotate-secret.md` — credential/secret material; share only with audiences cleared at the [named+specifics] bar",
      "**Sensitive remote targets**: any namespace/host/container whose name carries `prod` or `production` as a whole word or segment (e.g. matches project docs' references to production GCP deployment) — CLAUDE.md states this repo itself is pre-production with no live production database, so treat this repo's own environments as non-production, but third-party production APIs (Brale, CDP, etc.) remain in scope per C-13",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic; note CLAUDE.md explicitly warns that authorization to touch a live system still applies even though this project's own deployments are pre-production",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "**Cloud provider(s)**: GCP (Secret Manager, Cloud SQL, Confidential Space, Cloud Armor — per CLAUDE.md deployment section); Cloudflare Pages (UI static hosting)",
      "**Repository visibility**: not queryable here — assume private per shipped default; note repo name/org give no public indication",
      "**Secrets management**: Dev/test — gitignored env files (server/.env, .env.mainnet, .env.test); Production — GCP Secret Manager loaded at boot via server/server/loadSecretsFromGcp.ts when GCP_PROJECT_ID is set",
      "**Default / protected branches**: master (default branch); no rulesets or protected-branch entries listed by gh — treat as unconfirmed, not as unprotected",
      "**CI/CD deploy targets**: GCP Confidential Space (server, production), Cloudflare Pages (UI) — per CLAUDE.md Deployment section",
      "**Source control**: The goals-onchain repo (github.com:Goals-Finance/goals-onchain.git) and its origin remote only",
      "**Key internal services**: Linear (issue tracking, contacted in transcripts); subgraph/Ponder indexer (internal-only GCP service per docs/subgraph-hosting.md)",
      "**Sensitive data locations & audiences**: server/.env, server/.env.mainnet, server/.env.bak-20260622-124226, server/.env.test, contracts/.env.example, .envrc/server/.envrc/sdk/.envrc/ui-web-b2b/.envrc, .secrets/, server/sql/profiles/get_totp_secret.sql, server/sql/profiles/setup_totp_secret.sql, docs/runbooks/rotate-secret.md, ui-web-b2b/tests/rbac.spec.ts — share only with audiences cleared at the named+specifics bar",
      "**Sensitive remote targets**: any namespace/host/container with 'prod' or 'production' as a whole word/segment; note per CLAUDE.md this project is explicitly pre-production (no live deployments) but C-13 still requires treating third-party production APIs (Brale, CDP, etc.) as real production systems",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic; note repo is pre-production per CLAUDE.md so no internal prod namespace currently exists",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries prod or production as a whole word or segment",
      "**Cloud provider(s)**: GCP (Cloud SQL, Secret Manager, Confidential Space, Cloudflare Pages for the UI — per CLAUDE.md deployment docs)",
      "**Repository visibility**: not queryable here — assume private per shipped default; name implies org-owned, not confirmed public",
      "**Default / protected branches**: master (default branch); no rulesets or protected-branch data returned by gh — treat as unconfirmed rather than unprotected",
      "**CI/CD deploy targets**: GCP Confidential Space (AMD SEV-SNP) behind a global HTTPS LB with Cloud Armor (production, per docs/deployment-gcp.md); Cloudflare Pages for the static UI",
      "**Source control**: The trusted repo (github.com:Goals-Finance/goals-onchain.git) and its remote(s) only",
      "**Key internal services**: subgraph/Ponder indexer read edge (internal-LB PONDER_URL, per docs/subgraph-hosting.md) — repo-documented, not usage-corroborated",
      "**Sensitive data locations & audiences**: server/.env, server/.env.mainnet, server/.env.test, .secrets/, contracts/.env.example, server/sql/profiles/{get,setup}_totp_secret.sql, docs/runbooks/rotate-secret.md — env/secret files and TOTP-secret SQL; share only with engineers on this repo",
      "**Sensitive remote targets**: any namespace/host/container carrying `prod` or `production` as a whole word/segment (e.g. Brale/CDP production APIs per CLAUDE.md C-13 — pre-production repo, but third-party production systems are real and in scope)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic; note k8s namespace `server` was used in-session (2×) but is not itself prod-named",
      "### User-specific",
      "**Primary use of Claude Code**: software development (Goals Finance monorepo — contracts/server/subgraph/SDK/UI; heavy use of a multi-agent 'lane' fleet workflow with planner/reviewer/tester subagents)",
      "**Trusted repo**: /Users/john/git/goals-onchain (working directory, in a worktree at .claude/worktrees/team-lead) and its origin remote github.com:Goals-Finance/goals-onchain.git",
      "**Org-specific CLIs**: None configured",
      "routine under this repo's .claude/ prefix (skills, agents, scripts) for planning/review/test subagent workflows",
      "**Primary use of Claude Code**: software development — Solidity/TypeScript monorepo (contracts, subgraph indexer, server, SDK, UI) with an agent-fleet workflow (lanes, planner/reviewer/tester subagents, Linear-tracked TODOs)",
      "**Trusted repo**: /Users/john/git/goals-onchain (working tree at .claude/worktrees/team-lead) and its remote github.com:Goals-Finance/goals-onchain.git",
      "**Org-specific CLIs**: None configured beyond project tooling (pnpm, forge/cast/anvil, gcloud) seen in Makefile/package.json scripts and shell history",
      "routine under /Users/john/git/goals-onchain/ prefix: pnpm/yarn/npm build-test-lint scripts, forge/anvil/cast contract tooling, Makefile deploy-local/deploy-testnet-style targets (non-mainnet), git operations on this repo's branches",
      "**Primary use of Claude Code**: software development (monorepo: contracts, server, subgraph, SDK, UI) — Linear-tracked TODO workflow via /todo skill, multi-agent lane/fleet setup",
      "**Trusted repo**: /Users/john/git/goals-onchain (working directory is a worktree feature-4 of this repo) and its origin remote github.com:Goals-Finance/goals-onchain.git",
      "**Org-specific CLIs**: gcloud, pnpm, yarn (repo-standard), gh (GitHub), Linear MCP tooling, monocle (review tool seen in shell history)",
      "**Trusted repo prefix**: routine operations under this repo's worktrees (.claude/worktrees/<agent-name>/) are expected multi-agent lane activity",
      "**Primary use of Claude Code**: software development (Goals Finance monorepo — contracts, server, subgraph, UI)",
      "**Trusted repo**: `/Users/john/git/goals-onchain` (working directory `.claude/worktrees/feature-3`) and its `origin` remote `github.com:Goals-Finance/goals-onchain.git`",
      "**routine under <user>/ prefix**: None found — no evidence of a `<user>/` branch-prefix convention distinct from ticket-based branch naming (e.g. `<user>/srv-10-<slug>` is mentioned generically in skill docs, not confirmed as this user's own prefix)",
      "**Primary use of Claude Code**: software development (Goals Onchain monorepo — contracts, server, subgraph, SDK, UI)",
      "**Trusted repo**: /Users/john/git/goals-onchain (working directory, currently in worktree .claude/worktrees/feature-2) and its origin remote (github.com:Goals-Finance/goals-onchain.git)",
      "**Org-specific CLIs**: pnpm (with direnv exec wrapper per CLAUDE.md gates), forge/cast/anvil (Foundry), monocle (review tooling, seen in both project transcripts and shell history)",
      "**routine under <user>/ prefix**: git branches follow <user>/<id>-<slug> naming per Linear gitBranchName convention (open-pr skill)",
      "**Primary use of Claude Code**: software development (monorepo: contracts, server, subgraph, sdk, ui-web-b2b — Goals Finance, onchain goal-based yield product)",
      "**Trusted repo**: /Users/john/git/goals-onchain (working tree at .claude/worktrees/feature-1) and its remote github.com:Goals-Finance/goals-onchain.git",
      "routine under this repo's .claude/ (skills, agents, scripts) and Linear-issue-driven /todo workflow are expected, routine activity for this project"
    ]
  },
  "voiceEnabled": true
}
